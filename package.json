{
  "name": "nomuman.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
